{{- if .Values.wsGateway.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dg-platform-agent.fullname" . }}-ws-gateway
  labels:
    {{- include "dg-platform-agent.wsGateway.labels" . | nindent 4 }}
  {{- with .Values.wsGateway.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.wsGateway.ingress.className }}
  ingressClassName: {{ .Values.wsGateway.ingress.className }}
  {{- end }}
  {{- if .Values.wsGateway.ingress.tls }}
  tls:
    {{- range .Values.wsGateway.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.wsGateway.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "dg-platform-agent.fullname" $ }}-ws-gateway
                port:
                  number: {{ $.Values.wsGateway.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
