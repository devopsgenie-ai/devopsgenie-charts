apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dg-platform-agent.fullname" . }}-agent-pod-config
  labels:
    {{- include "dg-platform-agent.labels" . | nindent 4 }}
data:
  AGENT_POD_IMAGE: {{ include "dg-platform-agent.agentPod.imageRepository" . | quote }}
  AGENT_POD_TAG: {{ include "dg-platform-agent.agentPod.imageTag" . | quote }}
