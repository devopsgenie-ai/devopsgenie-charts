{{/*
Auto-create private registry pull secret from imageCredentials.username/token.
Skipped when using imageCredentials.existingSecret.
*/}}
{{- if and .Values.imageCredentials.username .Values.imageCredentials.token (not .Values.imageCredentials.existingSecret) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dg-platform-agent.imagePullSecretName" . }}
  labels:
    {{- include "dg-platform-agent.labels" . | nindent 4 }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ printf `{"auths":{"%s":{"username":"%s","password":"%s","auth":"%s"}}}` .Values.imageCredentials.registry .Values.imageCredentials.username .Values.imageCredentials.token (printf "%s:%s" .Values.imageCredentials.username .Values.imageCredentials.token | b64enc) | b64enc | quote }}
{{- end }}
