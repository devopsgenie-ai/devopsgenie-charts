apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "dg-platform-agent.fullname" . }}-controller
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dg-platform-agent.controller.labels" . | nindent 4 }}
rules:
  - apiGroups: [agents.x-k8s.io]
    resources: [sandboxes, sandboxtemplates]
    verbs: [get, list, watch, create, delete]
  - apiGroups: [extensions.agents.x-k8s.io]
    resources: [sandboxclaims]
    verbs: [get, list, watch, create, delete, patch]
  - apiGroups: [""]
    resources: [pods]
    verbs: [get, list, watch]
  # Services + port-forward: required by the k8s-agent-sandbox SDK tunnel
  # (controller reaches sandbox pods via sandbox-router-svc).
  - apiGroups: [""]
    resources: [services, services/proxy]
    verbs: [get, list, create]
  - apiGroups: [""]
    resources: [pods/portforward]
    verbs: [create]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "dg-platform-agent.fullname" . }}-controller
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dg-platform-agent.controller.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "dg-platform-agent.fullname" . }}-controller
subjects:
  - kind: ServiceAccount
    name: {{ include "dg-platform-agent.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
